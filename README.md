1. Clone the repo
2. Copy frontend/.env.local.example → frontend/.env.local and fill in values
3. Copy backend/.env.example → backend/.env and fill in values
4. npm install in both /frontend and /backend
5. Run frontend: npm run dev
6. Run backend: npm run dev